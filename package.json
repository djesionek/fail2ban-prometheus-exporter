{
  "name": "fail2ban-prometheus-exporter",
  "description": "Minimal exporter for fail2ban statistics for prometheus",
  "version": "0.0.1",
  "dependencies": {
    "minimist": "*",
    "geoip-lite": "*"
  }
}